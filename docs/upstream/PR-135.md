<!-- markdownlint-disable MD013 -->

# Pull request handoff: resolve native inspect lifecycle identities

## Summary

- Resolve `container inspect` targets from the atomic lifecycle view.
- Accept immutable lifecycle IDs, canonical names, and native bundle keys.
- Keep one output object when multiple supplied aliases identify the same container.
- Retain the existing not-found failure for unknown identities.

## Validation

- `swift test --scratch-path /Volumes/SSD/github/container-lifecycle-discovery/.build --filter ContainerInspectCommandTests`: 2 focused tests passed.
- The focused host-namespace fixture reached live inspection and teardown with the immutable ID returned by `compose ps -q`.
- The diagnostic debug-build timing was 12.383s for candidate startup versus 0.172s for Docker and remains deliberately open for the release-build performance gate.

## Scope

This change does not alter lifecycle identity generation, Docker route aliases, Compose output, or runtime resource identity. It only aligns the native inspection command with the already-delivered lifecycle identity contract.

Pull request: [`stephenlclarke/container#135`](https://github.com/stephenlclarke/container/pull/135).

Related issue handoff: `docs/upstream/ISSUE-120.md`.

<!-- markdownlint-enable MD013 -->
