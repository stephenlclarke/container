# Pull request 181: merge Apple system status enhancement

## Summary

Pull request
[`stephenlclarke/container#181`](https://github.com/stephenlclarke/container/pull/181)
merges Apple's richer `container system status` output while preserving the
fork-specific Container Engine and builder-shim provenance fields.

Companion tracking issue:
[`stephenlclarke/container#180`](https://github.com/stephenlclarke/container/issues/180).

## Apple-shaped boundary

- Retains Apple commit `d925dab8` as the second parent of one signed ancestry
  merge.
- Keeps Apple's grouped JSON schema, table fields, resource-count collection,
  API server version correction, and focused test suite.
- Limits manual reconciliation to existing fork functionality: Engine service
  status/socket data and builder-shim provenance.
- Does not add Compose-specific behavior to the Apple command.

## Review corrections

The first exact-head review found two real best-effort collection defects:

- the image-list XPC request had no response timeout and could block status
  output indefinitely;
- a successful image count was discarded whenever container listing failed.

The corrected head exposes a timeout-capable image-list call, bounds this
diagnostic probe to 10 seconds, and represents the container and image counts
independently so either successful probe remains visible.

## Evidence

- The focused `SystemStatusTests` suite covers the merged status contract and
  both one-sided resource-query failure cases.
- The focused suite covers running and unavailable states, JSON round-trip,
  table rendering, resource counts, builder-shim identity, and Engine fields.
- The staged merge has no conflict markers or whitespace errors.
- The downstream Compose exact pin, strict parity, packaging, quality, and
  release gates remain release-level requirements after this PR merges.

## Related work

- [`apple/container#815`](https://github.com/apple/container/issues/815)
- [`apple/container#1769`](https://github.com/apple/container/pull/1769)
- [`stephenlclarke/container#180`](https://github.com/stephenlclarke/container/issues/180)
- [`stephenlclarke/container-compose#348`](https://github.com/stephenlclarke/container-compose/pull/348)

Closes
[`stephenlclarke/container#180`](https://github.com/stephenlclarke/container/issues/180).
