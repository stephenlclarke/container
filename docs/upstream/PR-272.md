# Pull request 272: refresh matched runtime dependencies

## Summary

- Advance the exact Containerization revision used by the enhanced fork.
- Advance the builder-shim source revision, image tag, and immutable digest as
  one provenance record.
- Refresh SwiftPM resolution for the updated package graph.

## Type of change

- [x] Bug fix
- [ ] New feature
- [ ] Breaking change
- [x] Documentation update

## Motivation and context

The stale revisions prevent Container Compose from selecting the current
protected component branches as one resolvable release graph. The builder
metadata also needs to identify the image produced by the same exact source
commit.

## Testing

- [x] SwiftPM resolves the exact checked-in Containerization revision.
- [x] The 35 focused configuration-loader tests pass with the new immutable
  builder default.
- [x] Protobuf generation selects the exact commit that produced the builder
  image.
- [x] Swift formatting, package description, Markdown, and whitespace checks
  pass.
- [ ] Exact-head pull-request checks and review pass.

## Compatibility impact

The selected revisions add already-reviewed quality and documentation changes.
The package retains the stock Apple release path and its tagged dependencies.

## Remaining risks

Container Compose must consume the merged Container commit and prove the full
matched stack through its hosted Prebuilt Binaries workflow.

## Container checks

- [x] Added matching issue and pull-request handoff records.
- [x] Preserved Apple repositories as read-only upstreams.
- [x] Kept immutable image-digest authority.
- [x] Used a signed Conventional Commit and Conventional Commit pull-request
  title.
- [x] Included no credentials, private-key material, passwords, personal data,
  or private registry details.

Closes [#271](https://github.com/stephenlclarke/container/issues/271).

Implementation pull request: [#272](https://github.com/stephenlclarke/container/pull/272).
