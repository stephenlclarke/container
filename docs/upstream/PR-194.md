# Pull request 194: pin the deterministic Containerization NBD test

## Change

Pull request [#194](https://github.com/stephenlclarke/container/pull/194)
updates the immutable Containerization dependency from
`ddcfe35457c4af7b18452c3de1ea64bd171fc5f2` to the reviewed merge
`7d325176c08d45ca88be2761726bb2e07ed9dc94` from
[Containerization #66](https://github.com/stephenlclarke/containerization/pull/66).

That lower-repository change serializes one concurrency-sensitive integration
case. It does not modify Containerization library or runtime source, so this
pull request deliberately changes only `Package.swift`, `Package.resolved`,
and the issue/PR handoff.

## Validation

- `swift package resolve --disable-sandbox` selects the exact new revision and
  leaves every other resolved dependency unchanged.
- `swift build --target ContainerRuntimeClient
  --disable-automatic-resolution` passes.
- `git diff --check` passes.
- Hosted required checks and exact-head review are required before merge.

Related issue: [#193](https://github.com/stephenlclarke/container/issues/193).
Related Compose issue:
[container-compose #434](https://github.com/stephenlclarke/container-compose/issues/434).
