# Pull request 169: sync Apple main for 0.14.0

## Summary

Pull request
[`stephenlclarke/container#169`](https://github.com/stephenlclarke/container/pull/169)
merges Apple `main` through `388d964f`, carries Apple pull request 2138's tmpfs
repair, imports Apple pull request 2154's Container skill, and aligns both the
dependency graph and user documentation with the supported 0.14.0 stack.

Companion tracking issue:
[`stephenlclarke/container#168`](https://github.com/stephenlclarke/container/issues/168).

## Review corrections

The review-to-clean loop found and fixed real integration and documentation
defects:

- Apple's new tmpfs tests needed exhaustive handling for this fork's image
  mount result.
- The dependency graph still selected a pre-refresh SwiftNIO SSL revision.
- The imported skill omitted shipped attach, top, pause, unpause, events,
  resize, adaptive memory, shared-VM, DNS, Compose, and fork-install behavior.
- Machine examples used images without a compatible init, conflated the Linux
  home with the separately mounted macOS home, and omitted host working-directory
  inheritance.
- Network and volume compatibility claims were broader than the implemented
  subcommands.
- Docker's memory-only update path was not mapped to the supported live resize
  command and its dedicated-VM bounds.

Each inline finding has an explicit response and is resolved only after its fix
is present on the pull-request branch.

## Candidate evidence

- Exact dependency resolution selects the reviewed Containerization and
  SwiftNIO SSL release revisions.
- The final Containerization authority is merge commit
  `4d07c76bafabc0adbb1b394f454d5c957023a213`, whose Linux, macOS,
  integration, signature, CodeQL, and exact-head review gates passed in pull
  request 54.
- The focused command and parser suite passes 211 tests.
- Strict Swift formatting passes for the adapted parser test.
- Built CLI help confirms the documented native command and option surfaces.
- Plugin metadata parses with `jq`.
- Markdown and `git diff --check` gates pass.
- One exact-head full CI run and one exact-head clean review are required after
  the final Containerization merge revision is pinned.

## Related work

- [`stephenlclarke/containerization#54`](https://github.com/stephenlclarke/containerization/pull/54)
- [`stephenlclarke/swift-nio-ssl#2`](https://github.com/stephenlclarke/swift-nio-ssl/pull/2)
- [`stephenlclarke/container-compose#333`](https://github.com/stephenlclarke/container-compose/pull/333)
- [`apple/container#1809`](https://github.com/apple/container/pull/1809)
- [`apple/container#2138`](https://github.com/apple/container/pull/2138)
- [`apple/container#2154`](https://github.com/apple/container/pull/2154)

Closes
[`stephenlclarke/container#168`](https://github.com/stephenlclarke/container/issues/168).
