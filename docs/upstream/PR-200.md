# Pull request 200: sync Apple clean reliability updates before 0.14.1

## Change

Pull request [#200](https://github.com/stephenlclarke/container/pull/200)
merges Apple `container` main through
`025f57c6c0fed55bc155af0ddc13b11fff6f22e6`. It preserves the support fork's
complete history while importing Apple's read-only `container clean` repair
and current pinned release action.

## Integration review

Apple's runtime change applies directly to the fork's existing clean route. It
omits the root filesystem when the container is read-only, omits read-only
block mounts, attempts every writable trim target, and reports all failures in
one error. The surrounding fork-owned lifecycle, isolation, memory, logging,
Engine API, and prewarm code is unchanged.

The release-workflow update changes only the immutable SHA for
`softprops/action-gh-release` from 3.0.2 to 3.0.3.

## Validation

- Apple main is an ancestor of the candidate head and the branch is zero
  commits behind upstream.
- Swift formatting and `git diff --check` pass for the imported source.
- The modified RuntimeLinux target compiles against the matched dependency
  graph.
- Both release workflow YAML and the focused clean integration-test source are
  validated before hosted required checks.
- Exact-head review must report no unresolved findings before merge.

Related issue: [#199](https://github.com/stephenlclarke/container/issues/199).
