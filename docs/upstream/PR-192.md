# Pull request 192: sync Apple updates before 0.14.1

## Change

Pull request [#192](https://github.com/stephenlclarke/container/pull/192)
merges current Apple `container` main into the support fork. It integrates the
new `container clean` command and Containerization 0.43 dependency while
preserving the fork's attach, process inspection, lifecycle, logging, Engine
API, isolation, prewarm, and memory-control extensions.

The matched dependency is the reviewed Containerization merge from
[containerization #64](https://github.com/stephenlclarke/containerization/pull/64)
at `ddcfe35457c4af7b18452c3de1ea64bd171fc5f2`.

## Conflict review

Package metadata retained the fork-owned remote graph and builder identity.
The command registry includes both existing `attach` and new `clean` commands.
Runtime clients retain process inspection and add the clean route. The service
keeps fork lifecycle serialization while exposing Apple's clean operation.

## Validation

- The `container` product compiles against the synchronized Containerization
  source.
- Remote package resolution selects the merged Containerization revision.
- `git diff --check` and commit-signature checks pass.
- Hosted required checks and exact-head Codex review are required before merge.

Related issue: [#191](https://github.com/stephenlclarke/container/issues/191).
