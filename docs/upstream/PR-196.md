# Pull request 196: pin the reviewed Containerization upstream sync

## Change

Pull request [#196](https://github.com/stephenlclarke/container/pull/196)
updates the immutable Containerization dependency from
`7d325176c08d45ca88be2761726bb2e07ed9dc94` to the reviewed merge
`f77f8d2833b54498546c081a0a5d15b4751b62bd` from
[Containerization #68](https://github.com/stephenlclarke/containerization/pull/68).

That lower-repository change synchronizes Apple pull requests #843 and #855
and includes the exact-head fixes required for safe reusable vsock-slot
ownership.

The pull request also closes the repository's only open Dependabot alert by
updating the semantic helper from `google.golang.org/grpc` 1.82.1 to the fixed
1.83.1 release and refreshing its compatible indirect module graph. Container
library and runtime source remain unchanged.

## Validation

- `swift package resolve --disable-sandbox` selects the exact new revision and
  leaves every other resolved dependency unchanged.
- `swift build --target ContainerRuntimeClient
  --disable-automatic-resolution` passes.
- `go test ./...` passes in `Tools/ContainerSemanticHelper`.
- `git diff --check` passes.
- Hosted required checks and exact-head review are required before merge.

Related issue: [#195](https://github.com/stephenlclarke/container/issues/195).
