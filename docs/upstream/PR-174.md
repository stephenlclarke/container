# Pull request 174: pin the VZ lifecycle repair

## Summary

- Advance Containerization from `59ce8dafa118` to the signed pull request 60
  merge `e5a92e86bf03`.
- Refresh only the SwiftPM origin hash and Containerization revision.
- Preserve every unrelated dependency revision in the 0.14.0 graph.

Tracking pull request:
[`stephenlclarke/container#174`](https://github.com/stephenlclarke/container/pull/174).

## Functional reason

The old graph reproduces three release-blocking VZ pod lifecycle failures.
The selected Containerization merge provides race-free pidfd signaling for
repeated stop/NBD cleanup and mount-ID-bound, no-symlink `openat2` resolution
for hotplugged block root filesystems.

## Evidence

- Containerization exact head `524035569679` passed clean connector review,
  all review threads were answered and resolved, and it merged as
  `e5a92e86bf03`.
- The three previously failing VZ integration contracts passed 3/3.
- Containerization macOS and Linux builds, unit tests, guest initfs build, and
  release CodeQL passed.
- Container SwiftPM resolution selects `e5a92e86bf03` and changes no unrelated
  pin.
- The affected `ContainerRuntimeClient` target builds with warnings treated as
  errors using SSD-backed scratch storage.

## Release boundary

This pull request changes only the lower runtime authority consumed by
Container. Compose owns the upper graph pin and stable 0.14.0 publication.
